// Menggunakan perulangan for untuk mengakses elemen array
  let buah = ["Mangga", "Salak", "Jeruk", "Anggur"];
  for (let i = 0; i < buah.length; i++) {
    console.log("Buah ke-" + (i + 1) + ": " + buah[i]);
}
